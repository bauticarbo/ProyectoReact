import React from "react";

const CartWidget = () => {
  return (
    <div>
      <span id="addCart" class="material-icons">
        shopping_cart
      </span>
      <span id="nroCarrito">3</span>
    </div>
  );
};

export default CartWidget;
